[build]
builder = "NIXPACKS"
buildCommand = "mvn clean package -DskipTests"

[build.nixpacksPlan.phases.setup]
nixPkgs = ["jdk21", "maven"]

[deploy]
startCommand = "java -jar target/hms-backend-1.0.0.jar"
restartPolicyType = "ON_FAILURE"


